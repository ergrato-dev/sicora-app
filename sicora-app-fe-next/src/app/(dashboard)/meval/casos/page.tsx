import CasosContent from './CasosContent';

export const metadata = {
  title: 'Casos Disciplinarios | SICORA',
  description: 'Gestión de casos disciplinarios de estudiantes',
};

export default function CasosPage() {
  return <CasosContent />;
}
