import EntregasContent from './EntregasContent';

export const metadata = {
  title: 'Entregas | SICORA',
  description: 'Gestión de entregas de proyectos formativos',
};

export default function EntregasPage() {
  return <EntregasContent />;
}
