# =============================================================================
# ⚠️  DEPRECADO - Este archivo ya no se usa
# =============================================================================
# Fecha de deprecación: Enero 2026
# Motivo: Migración a Traefik como reverse proxy
# 
# Nueva configuración: sicora-infra/traefik/
# ADR: _docs/configuracion/ADR_TRAEFIK_REVERSE_PROXY.md
#
# Si necesitas usar Nginx por algún motivo específico, este archivo
# se mantiene como referencia histórica.
# =============================================================================

server {
    listen 80;
    server_name _;  # Cambiar por tu dominio

    # Configuración de logs
    access_log /var/log/nginx/sicora_access.log;
    error_log /var/log/nginx/sicora_error.log;

    # Configuración general
    client_max_body_size 10M;

    # API Gateway
    location /api/ {
        proxy_pass http://localhost:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Configuración de timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Notification Service
    location /notifications/ {
        proxy_pass http://localhost:8001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Configuración de timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Health checks
    location /health/api {
        proxy_pass http://localhost:8000/health;
        access_log off;
    }

    location /health/notification {
        proxy_pass http://localhost:8001/health;
        access_log off;
    }

    # Documentación Swagger
    location /api/docs {
        proxy_pass http://localhost:8000/docs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /notifications/docs {
        proxy_pass http://localhost:8001/docs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Página de estado básica
    location /status {
        access_log off;
        return 200 "SICORA Backend OK\n";
        add_header Content-Type text/plain;
    }
}
